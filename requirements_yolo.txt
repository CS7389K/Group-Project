# YOLO11 + VLM System Requirements
# Install on Jetson Xavier NX

# Core YOLO11
ultralytics>=8.3.0

# Already installed (from existing system):
# - opencv-python (cv2)
# - numpy
# - torch
# - transformers
# - Pillow (PIL)
# - requests
# - flask

# For ROS2:
# Install via apt (not pip):
#   sudo apt install ros-humble-cv-bridge
#   sudo apt install ros-humble-sensor-msgs
